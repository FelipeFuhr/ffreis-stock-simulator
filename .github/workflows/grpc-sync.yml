name: gRPC Sync

on:
  push:
    branches: ["**"]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  grpc-sync-and-parity:
    timeout-minutes: 15
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-run-commands.yml@main
    with:
      setup-python: true
      python-version: "3.13"
      setup-uv: true
      timeout-minutes: 30
      commands: |-
        set -euo pipefail
        uv sync --frozen --extra dev --extra api --extra grpc
        make grpc-check
        make test-grpc-parity
        uv run --frozen pytest -q tests/integration_tests/test_live_interface_parity.py
